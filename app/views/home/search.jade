extends ../layout

block content
	h1 Result here.

	for release in releaseGroups
		div.singleresult Release
			h3 #{release.title}
			p #{release.mbid}
			p #{release.type}
			p #{release.artist_name}
			img(src=release.img_url, height='200px', width='200px')